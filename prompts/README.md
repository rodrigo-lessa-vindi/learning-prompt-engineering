# Prompts
This is where all instruction sets live. Using Markdown (.md) files for readability.

## Default behavior
- default_system_instructions.md (A foundational, general instruction set that you might pass to every agent for baseline behavior)